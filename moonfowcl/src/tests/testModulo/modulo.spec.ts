describe('Test Modulo Cambio Residenza', function() {
  /*
  var tabNuovoModulo =  element(by.id('ngb-nav-4'));


  beforeEach(function() {
    browser.get('http://mydevpc.csi.it:17000/moonfowcl/');
  });


  it('should have a title', function() {
    expect(browser.getTitle()).toEqual('MOOn - CSI Piemonte');
  });

  it('Nuovo Modulo', function() {
    tabNuovoModulo.click();
    expect(element.all(by.css('.card-link')).count()).toEqual(11);
  });
  */
});
